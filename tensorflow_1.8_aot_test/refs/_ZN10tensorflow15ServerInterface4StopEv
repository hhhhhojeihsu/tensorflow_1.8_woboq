<dec f='tensorflow/tensorflow/core/distributed_runtime/server_lib.h' l='50' type='tensorflow::Status tensorflow::ServerInterface::Stop()'/>
<doc f='tensorflow/tensorflow/core/distributed_runtime/server_lib.h' l='45'>// Stops the server asynchronously. Returns OK on success, otherwise returns
  // an error.
  //
  // After calling `Stop()`, the caller may call `Join()` to block until the
  // server has stopped.</doc>
