<def f='tensorflow/tensorflow/compiler/xla/client/xla_client/xla_builder.h' l='793' type='tensorflow::Status xla::XlaBuilder::first_error() const'/>
<doc f='tensorflow/tensorflow/compiler/xla/client/xla_client/xla_builder.h' l='787'>// Returns the first error that was encountered while building the
  // computation. When an error is encountered, by default we return a vacuous
  // XlaOp and inform the user of the error that occurred while
  // building the computation when they make a final call to Build().
  //
  // See also set_die_immediately_on_error().</doc>
