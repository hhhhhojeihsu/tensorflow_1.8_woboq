<def f='tensorflow/tensorflow/core/kernels/batching_util/shared_batch_scheduler.h' l='131' ll='160'/>
<doc f='tensorflow/tensorflow/core/kernels/batching_util/shared_batch_scheduler.h' l='125'>// Adds a queue to which tasks may be submitted. The returned queue implements
  // the BatchScheduler API. Each queue has its own set of scheduling options,
  // and its own callback to process batches of tasks submitted to the queue.
  //
  // The returned queue&apos;s destructor blocks until all tasks submitted to it have
  // been processed.</doc>
<mbr r='tensorflow::serving::SharedBatchScheduler::QueueOptions::max_batch_size' t='int'/>
<mbr r='tensorflow::serving::SharedBatchScheduler::QueueOptions::batch_timeout_micros' t='int64'/>
<mbr r='tensorflow::serving::SharedBatchScheduler::QueueOptions::max_enqueued_batches' t='int'/>
