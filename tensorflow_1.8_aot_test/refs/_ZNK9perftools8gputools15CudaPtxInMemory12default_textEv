<dec f='tensorflow/tensorflow/stream_executor/kernel_spec.h' l='177' type='const char * perftools::gputools::CudaPtxInMemory::default_text() const'/>
<doc f='tensorflow/tensorflow/stream_executor/kernel_spec.h' l='172'>// Returns pointer to the ptx of available implementation with the
  // lowest-valued compute capability. For example, if PTX written to CC2.0,
  // 3.0, and 3.5 are all available, the version for CC2.0 will be set. Returns
  // nullptr on failed lookup (if any version is not available).
  // When the ptx is compressed, returns the decompressed ptx.</doc>
<def f='tensorflow/tensorflow/stream_executor/kernel_spec.cc' l='93' ll='112' type='const char * perftools::gputools::CudaPtxInMemory::default_text() const'/>
