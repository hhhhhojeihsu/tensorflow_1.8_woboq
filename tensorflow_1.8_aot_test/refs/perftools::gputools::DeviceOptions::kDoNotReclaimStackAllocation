<dec f='tensorflow/tensorflow/stream_executor/device_options.h' l='43' type='const unsigned int'/>
<doc f='tensorflow/tensorflow/stream_executor/device_options.h' l='39'>// When it is observed that more memory has to be allocated for thread stacks,
  // this flag prevents it from ever being deallocated. Potentially saves
  // thrashing the thread stack memory allocation, but at the potential cost of
  // some memory space.</doc>
